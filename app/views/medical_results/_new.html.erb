
<div class="modal fade" id="new_medical_result_modal" tabindex="-1" role="dialog">
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content" id="box-over">
      <div class="modal-header">
        <div>
    <img class="img-boxover"src="https://www.eurotransportcar.com/bundles/front/images/loading.gif" alt="sending"></div>
        <h5 class="modal-title"><strong>Agregar un resultado médico</strong></h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <%= simple_form_for [@record, @medical_result], remote: true do |f| %>
          <%= f.error_notification %>
          <%= if f.object.errors[:base].present?
                f.error_notification message: f.object.errors[:base].to_sentence
              end %>

          <div class="form-inputs form1">
            <%= f.input :title, label: "Nombre del archivo", placeholder: "Informe-0000" %>
            <%= f.input :photo, label: "Archivo o imagen", as: :file  %>
          </div>

          <div class="form-actions">
            <%= f.button :submit, value: "Agregar", class: "btn btn-flat" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
